plugins:
  ./plugins/transport:
    _priority: -2
    smtp:
      host: "localhost"
      port: 8888
  ./plugins/render:
    partialsPath: "{{CWD}}/test/partials/"
    helpersPath: "{{CWD}}/test/helpers/"
views:
  engines:
    html: "handlebars"
  path: "{{CWD}}/test/emails"
  isCached: false
